name: natatorio_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  # Paquetes de Firebase unificados y compatibles
  firebase_core: ^3.10.0
  firebase_auth: ^5.4.0
  cloud_firestore: ^5.6.0
  firebase_storage: ^12.4.0
  firebase_messaging: ^15.2.0

  # Resto de tus dependencias
  flutter_local_notifications: ^17.0.0
  cupertino_icons: ^1.0.8
  google_sign_in: ^6.2.1
  file_picker: ^11.0.2
  http: ^1.6.0
  uuid: ^4.5.3
  intl: ^0.20.3
  flutter_image_compress: ^2.3.0
  url_launcher: ^6.3.2
  pdf: ^3.12.0
  printing: ^5.14.3
  mobile_scanner: ^7.2.0
  cloud_functions: ^5.2.0
  image_picker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true